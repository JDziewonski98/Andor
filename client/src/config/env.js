/*
* Get the IP from whoever started the server.
* Put the IP in this file.
* In localhost:8080 go to the "site settings" 
* which you'll find in the "Not Secure" area and allow 
* insecure content from all the way at the bottom.
*/

module.exports = {
    BASE_API: "http://localhost:3000"
}